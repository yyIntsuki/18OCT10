@model _18OCT10.Models.Car

@{
    ViewBag.Title = "Rent";
}

<h2>Rent</h2>
<p>
    We have now @foreach (var cars in Model.RegistrationNum)
    {
        <div>@Console.WriteLine("Okay")</div> //Temporary, will change later.
    }
</p> // Writes how many cars there is available.
